# 更新日志

本项目的所有重要变更都将记录在此文件中。

## [2.3.3] - 2026-08-17

### 新增功能

#### AI 助手
- **AI 对话面板** - OpenAI 兼容客户端 + 配置管理，多轮对话感知终端上下文，历史持久化，流式输出 + Markdown 渲染 + 快捷提问，面板可拖拽宽度
- **智能体 Agent** - 自主多轮执行命令诊断服务器，自动滚动跟随最新回复，轮次上限保护，支持清空当前会话

#### Docker 管理
- **Docker 面板** - 容器/镜像/日志/容器终端（下沉到免费版），容器列表排序，日志直接在终端执行，面板关闭后自动降频释放资源

#### MCP 增强
- **MCP 日志体系** - 操作日志持久化，独立日志页面（分页/搜索），显示连接名称和操作图标，exec 命令结果可展开查看
- **Token 鉴权** - API 服务增加 Bearer Token 鉴权，token 自动同步到配置文件
- **协议增强** - SSE 传输模式 + resources（远程文件树）+ prompts（运维命令模板）
- **AI 操作联动** - MCP 建立连接后自动打开前端终端；AI 执行的命令和文件操作实时同步显示到终端（`[MCP]` 前缀），全程可见不隐身

#### 文件管理与终端
- **sudo 提权上传** - 目标目录无写权限时自动弹窗用 sudo 重试（SFTP 传 /tmp + sudo mv，密码通过 stdin 安全传输），MCP `iter_upload_file`/`iter_upload_folder` 支持 `use_sudo` 参数
- **文件编辑器** - CodeMirror 6 替换 TextArea，语法高亮 + 更完善的编辑体验
- **文件面板跟随终端** - `cd` 后文件面板自动切换到当前目录（OSC 7 跨 chunk 解析）
- **终端增强** - 终端内链接点击、多连接输入广播、文件夹下载、文件复制/移动、命令提示（ghost text）

#### 其他
- **Pro 功能** - 命令片段库、端口转发（Pro 版专属）

### 修复

- **macOS 输入法丢字符** - 修复活动 IME 下快速输入时随机丢字符/乱序（xterm `_inputEvent` 补丁，绕过上游 #5887）
- **Option 组合键脏字符** - 从 xterm `_inputEvent` 层拦截 dead-key 字符化泄漏，Option 快捷键（如 Option+H 历史弹窗）不再向终端注入 `˙` 等字符
- **历史命令弹窗脏字符** - 选中命令前发送 Ctrl+U 清行，替换时不再残留旧输入
- **CWD 钩子注入回显** - 重写过滤为 buffer + 正则方案，修复 PS2 提示符、ANSI 转义序列、正常内容卡缓冲等一系列问题
- **MCP 上传 No such file** - `upload_file_sync` 误用只读 flag 打开远程文件导致上传必然失败，改用 CREATE|TRUNCATE|WRITE
- **read_file 大文件截断** - 单次 read 只读一个 SFTP packet（约 32KB），大于 32KB 的文件静默只返回开头，改为循环读满
- **连接内存泄漏** - 修复连接关闭后约 118M 内存不释放
- **exec 日志中文 panic** - 日志按字节截断可能切在 UTF-8 字符中间导致崩溃，改按字符边界截断
- **Cmd+V 粘贴重复** - 修复 macOS 终端粘贴内容重复出现
- **AI 对话体验** - 流式输出自动滚动、用户手动上滚时停止跟随、运行按钮误显示、命令识别误判等

### 性能优化

- **MCP 调用链路** - exec 加 60s 超时 + 1MB 输出上限（超时返回已收到的输出，防 `tail -f` 等命令挂死 channel；多智能体并发不再互相拖垮）；上传/下载/压缩/解压不限时；操作日志写入异步化；SFTP 并发建连加锁防孤儿连接
- **前端代码分割** - 主 bundle 2.4M 降至 1.07M，CodeMirror 语言包按需加载
- **空闲 CPU 优化** - 移除空闲持续动画、focusin 事件委托替代全局 MutationObserver、窗口失焦/非活动连接暂停光标闪烁、xterm WebGL 渲染器
- **Docker 面板** - 修复轮询死循环和 exec channel 泄漏导致的持续高 CPU

## [2.3.2] - 2026-05-31

### 修复

- **连接重连失败** - 修复重连时因后端旧 SESSIONS 条目未清除导致连接创建失败的问题，重连前先调用 `disconnect_ssh` 清理旧会话
- **重连后命令历史** - 修复重连后命令历史无法正常记录的问题，补充重连监听器中的 `commandTracker` 逻辑

### 改进

- **输入框清空按钮** - 所有输入框增加 `allowClear` 清空按钮，方便一键清除输入内容

### 性能优化

- **CPU 占用优化** - 将 MutationObserver 从全文档扫描改为 rAF 批量处理 `addedNodes`，降低 CPU 占用
- **进度条帧率优化** - 进度条渲染帧率从 62fps 降至 20fps，减少不必要的重绘
- **CSS 死代码清理** - 删除 6 段无效 CSS 代码，`backdrop-filter` 减半，`gradient-text` 静态化
- **端口检测状态灯修复** - 修复端口检测状态灯显示异常的问题

## [2.3.1] - 2026-04-23

### 修复

- **连接拖拽排序空白** - 修复拖拽排序过程中目标位置 tab 内容变空白的问题
- **拖拽到新窗口检测** - 修复需要先往下拖再往上拖才能触发新窗口检测的问题
- **新窗口主题同步** - 修复新窗口中切换主题不生效的问题，支持跨窗口主题同步
- **拖拽到新窗口后 SSH 泄漏** - 修复拖拽到新窗口后原窗口 SSH 连接未断开的问题
- **应用重启连接恢复** - 修复重启后连接恢复丢失分屏结构和明文存储密码的问题

### 改进

- **上传文件夹按钮** - 文件管理工具栏添加"上传文件夹"按钮

## [2.3.0] - 2026-04-12

### 新增功能

#### 文件管理增强
- **拖拽上传优化** - 修复 macOS 上拖拽上传目标目录检测偏移问题
- **树形/列表视图优化** - 拖拽上传目标精确跟随鼠标位置
- **拖拽视觉反馈** - 保留目录选中时的虚线框高亮效果

#### 主题与外观
- **主题系统** - 支持亮色/暗色主题切换
- **自定义样式** - 优化全局 CSS 变量，统一主题配色

#### 启动体验
- **启动动画** - SplashScreen 组件，Logo 动画 + 进度条 + 步骤状态显示
- **静态预加载器** - 纯 HTML + CSS，避免 React 加载白屏
- **应用初始化优化** - 数据库/字体/连接预加载流程优化

### 改进

- **分屏终端适配** - 修复分屏终端内容尺寸适配问题，确保终端填满分屏区域
- 更新 README 应用截图，展示最新功能
- 优化文件管理器拖拽上传体验
- 改进 README 文档描述，更新核心特性

### 技术改进

- 修复 Tauri macOS 拖拽事件 Y 坐标偏移 Bug（添加 -19px 补偿）
- 优化文件管理视图渲染性能
- 改进终端分屏布局计算逻辑

---

## [2.2.0] - 2026-03-25

### 新增功能

#### 启动动画
- **SplashScreen 组件** - Logo 动画 + 进度条 + 步骤状态显示
- **静态预加载器** - 纯 HTML + CSS，避免 React 加载白屏
- **应用初始化流程** - 数据库/字体/历史迁移/连接预加载/过期清理
- **字体缓存策略** - localStorage 缓存字体列表，毫秒级启动
- **连接列表预加载** - 启动时加载所有连接到 store，全局共享

#### 命令历史追踪
- **Shell 输出解析** - 从终端输出中自动提取执行的命令
- **多场景支持** - 支持 Tab 补全、上下键历史导航、直接输入
- **提示符检测** - 支持多种 Shell 格式 (`[user@host path]$`、`user@host:path$`、`❯`)
- **控制字符过滤** - 自动过滤 `^C`、`^D`、`^Z` 等控制字符
- **Ghost Text 自动补全** - 根据历史命令显示建议，最多 10 条
- **历史命令弹窗** - 快捷键打开历史命令列表，支持搜索和清空
- **权重算法** - 70% 最近使用 + 30% 使用频率排序

#### 快捷键增强
- **快捷键设置** - 支持清空快捷键（Delete/Backspace）
- **历史命令快捷键** - 自定义快捷键打开历史弹窗

### 改进

- 移除调试日志，优化性能
- 清理未使用代码
- 重新设计连接断开重连逻辑

---

## [2.1.0] - 2026-03-24

### 新增功能

#### 终端增强
- **xterm.js 升级** - 升级到 xterm.js v6.0.0（@xterm/xterm），支持新特性和性能优化
- **终端滚动条优化** - 更细更美观的滚动条样式
- **终端配置动态更新** - scrollback 等配置实时生效，无需重启

#### 数据库改进
- **Dev/Prod 环境分离** - 开发和生产模式使用独立数据库文件，避免数据污染
- **WAL 模式** - 启用 SQLite WAL 模式，提升并发性能
- **事务保护** - 批量导入操作添加事务保护，防止数据丢失
- **加密密钥改进** - 使用机器唯一标识派生密钥，支持跨会话数据恢复
- **老数据自动迁移** - 自动将老版本加密的密码迁移到新密钥

#### 窗口管理
- **窗口状态插件** - 使用 tauri-plugin-window-state 管理窗口位置和最大化状态
- **多显示器优化** - 修复多显示器 DPI 缩放不一致导致的窗口大小异常

#### UI 改进
- **快速导入粘贴** - 修复快速导入输入框无法使用快捷键粘贴的问题
- **导出连接优化** - 添加全选/取消全选按钮，优化勾选框显示
- **复制信息** - 复制连接信息时显示明文密码
- **最近连接刷新** - 连接成功后最近连接列表立即更新

### 技术改进

- 新增 `tauri-plugin-window-state` 插件
- 新增 `hostname` 依赖用于机器标识
- 改进密码迁移逻辑，支持从 localStorage 和老数据库迁移
- 优化数据库错误处理，记录解析失败日志

---

## [2.0.0] - 2026-03-22

### 新增功能

#### 终端增强
- **终端分屏** - 支持水平/垂直分屏，可拖拽调整大小，拖拽会话标签创建新分屏
- **多窗口支持** - 拖拽连接标签到窗口边缘创建新窗口，窗口位置自动记忆
- **快捷键系统** - 完整的快捷键支持（分屏、新建会话、关闭会话、搜索等）
- **终端搜索增强** - 支持终端内容全文搜索
- **自动重连** - 断线后自动重连服务器

#### 文件管理
- **文件预览/编辑** - 支持文本文件在线预览和编辑
- **文件搜索** - 远程文件搜索功能
- **文件压缩/解压** - 支持 tar.gz/zip/gz 等格式压缩和解压
- **传输控制** - 支持上传下载暂停/恢复

#### 连接管理
- **SSH 密钥认证** - 支持 OpenSSH 和 PEM 格式密钥文件
- **连接导入/导出** - JSON 格式批量导入导出连接配置
- **连接排序** - 支持拖拽排序和批量管理
- **最近连接** - 记录最近连接历史，快速访问

#### 安全性
- **密码加密存储** - AES-256-GCM 加密存储密码
- **后端数据库** - 连接数据存储在后端 SQLite 数据库

#### MCP 服务
- **MCP 日志面板** - 实时显示 MCP 操作日志
- **日志换行显示** - 支持长命令换行显示
- **命令复制** - 一键复制执行的命令

#### 其他
- **终端主题配置** - 可自定义终端字体、字号、光标样式
- **窗口位置记忆** - 窗口位置在显示前恢复，无闪烁

### 技术改进

- 升级 React 19.x
- 升级 Tauri 2.x
- 使用 russh 0.50 + russh-sftp 2.1
- 前端测试覆盖 135 个测试用例
- E2E 测试支持

### MCP 工具

| 工具 | 说明 |
|------|------|
| `iter_status` | 检查 iTerminal API 服务状态 |
| `iter_connect` | 创建 SSH 连接 |
| `iter_disconnect` | 断开 SSH 连接 |
| `iter_test_connection` | 测试 SSH 连接 |
| `iter_list_connections` | 列出活跃连接 |
| `iter_exec` | 在远程服务器执行命令 |
| `iter_monitor` | 获取系统监控数据 |
| `iter_list_dir` | 列出远程目录 |
| `iter_mkdir` | 创建目录 |
| `iter_rm` | 删除文件 |
| `iter_rename` | 重命名文件/目录 |

---

## [1.0.0] - 2026-03-16

### 新增功能

- SSH 连接管理，支持多会话
- SFTP 文件浏览器，支持上传/下载/拖拽上传
- 实时系统监控（CPU、内存、磁盘）
- MCP 服务器集成，支持 AI 助手（Claude 等）
- 多标签终端，支持搜索功能
- 连接分组和标签管理
- 中文界面支持

### MCP 工具

| 工具 | 说明 |
|------|------|
| `iter_status` | 检查 iTerminal API 服务状态 |
| `iter_connect` | 创建 SSH 连接 |
| `iter_disconnect` | 断开 SSH 连接 |
| `iter_test_connection` | 测试 SSH 连接 |
| `iter_list_connections` | 列出活跃连接 |
| `iter_exec` | 在远程服务器执行命令 |
| `iter_monitor` | 获取系统监控数据 |
| `iter_list_dir` | 列出远程目录 |
| `iter_mkdir` | 创建目录 |
| `iter_rm` | 删除文件 |
| `iter_rename` | 重命名文件/目录 |

### 技术栈

- 前端：Tauri 2 + React 19 + TypeScript 5
- 后端：Rust + russh + russh-sftp
- MCP：Model Context Protocol SDK
- 跨平台：支持 macOS、Windows、Linux